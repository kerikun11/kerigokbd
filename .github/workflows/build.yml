name: Build QMK Firmware

on:
  push:
    paths:
      - 'software/qmk/**'
      - '.github/workflows/build.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install ARM toolchain
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y gcc-arm-none-eabi

      - name: Install QMK CLI
        run: |
          pip3 install qmk -r software/qmk/firmware/requirements.txt
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Build all keyboards
        run: ./software/qmk/qmk.sh -a

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: |
            software/qmk/firmware/kerigokbd_kerigokbd_v2_pcb_20260222_via.uf2
            software/qmk/firmware/kerigokbd_kerigokbd_v1_pcb_20260222_via.uf2
          if-no-files-found: error
